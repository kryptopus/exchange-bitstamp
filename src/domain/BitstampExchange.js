/* @flow */
import type {ExchangeInterface} from "kryptopus-exchange/src/domain/ExchangeInterface"

/**
 * Bitstamp exchange
 */
export default class BitstampExchange implements ExchangeInterface
{
}
